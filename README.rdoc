== STLP Newsletter Rails Application

This is a lightweight framework for creating the newsletter/highlight real for the Stanford Technical Leadership Program (STLP).

== Running Tests

There are minimal tests since most of this application is straight rails.  We use rspec for the minimal number of tests we have. To run them simply run:

    rspec

