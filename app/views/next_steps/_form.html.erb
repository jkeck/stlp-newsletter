<%= form_for(@next_step) do |f| %>
  <% if @next_step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@next_step.errors.count, "error") %> prohibited this next_step from being saved:</h2>

      <ul>
      <% @next_step.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :steps %><br />
    <%= f.text_area :steps %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
