<%= form_for(@misc_content) do |f| %>
  <% if @misc_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@misc_content.errors.count, "error") %> prohibited this misc_content from being saved:</h2>

      <ul>
      <% @misc_content.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
