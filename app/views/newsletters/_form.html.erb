<%= form_for(@newsletter) do |f| %>
  <% if @newsletter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newsletter.errors.count, "error") %> prohibited this newsletter from being saved:</h2>

      <ul>
      <% @newsletter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :session %><br />
    <%= f.text_field :session, :disabled => !@newsletter.session.blank? %>
  </div>
  <div class="field">
    <%= f.label :dates %><br />
    <%= f.text_field :dates %>
  </div>
  <div class="field">
    <%= f.label :year %><br />
    <%= f.text_field :year, :size => "4" %>
  </div>
  <div class="actions">
    <%= f.submit(:class=>"btn") %>
  </div>
<% end %>
