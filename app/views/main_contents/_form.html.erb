<%= form_for(@main_content) do |f| %>
  <% if @main_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@main_content.errors.count, "error") %> prohibited this main_content from being saved:</h2>

      <ul>
      <% @main_content.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br />
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :body_overlay %><br />
    <%= f.check_box :body_overlay %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
